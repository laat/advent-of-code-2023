(ns dayX
  (:require
   [aocd.core :as data]
   [clojure.string :as str]))

(def input (data/input 2022 1))
(def example "")

;; part1
(->> example
     str/split-lines)

;; part2
(->> example
     str/split-lines)
